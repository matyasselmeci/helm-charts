apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pelican-cache.fullname" . }}-test-connection"
  labels:
    {{- include "pelican-cache.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pelican-cache.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
